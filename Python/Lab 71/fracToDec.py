while(True):

    # Get the numerator
    numer = int(input("Enter a numerator :: "))


    # Get the denominator
    denom = int(input("Enter a denominator :: "))


    # convert the fraction
    decimal = numer/denom



    # Print the fraction and decimal value
    print(numer, " / ", denom, " = ", decimal)
